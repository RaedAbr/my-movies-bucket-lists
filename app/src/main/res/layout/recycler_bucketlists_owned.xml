<?xml version="1.0" encoding="utf-8"?>

<FrameLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_bucketlists_owned"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_bucketlist"
        tools:itemCount="2"/>

    <ProgressBar
        android:id="@+id/recycler_bucketlists_owned_progressbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <TextView
        android:id="@+id/recycler_owned_no_items"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/you_have_no_bucket_list"
        android:layout_gravity="center_vertical"
        android:layout_margin="8dp"
        android:visibility="gone"
        tools:visibility="visible"/>
</FrameLayout>