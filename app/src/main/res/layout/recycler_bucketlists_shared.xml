<?xml version="1.0" encoding="utf-8"?>

<FrameLayout
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_bucketlists_shared"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_bucketlist_owned"
        tools:itemCount="2"/>

    <ProgressBar
        android:id="@+id/recycler_bucketlists_shared_progressbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <TextView
        android:id="@+id/recycler_shared_no_items"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/no_bucket_lists_shared_with_you"
        android:layout_gravity="center_vertical"
        android:layout_margin="8dp"
        android:visibility="gone"
        tools:visibility="visible"/>
</FrameLayout>
